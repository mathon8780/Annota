# Findings

## 当前仓库状态
- `D:\Program\Annota\project` 当前为空，没有现存源码或 `AGENTS.md`。
- 外层目录包含 `Documents/`、`HTML-Demo/` 和空的 `project/`。

## 产品约束
- 首发仅 Windows；暂不做 Web/移动端。
- Local-first、BYOK；首版仅 OpenAI Compatible，不支持本地模型。
- 一个知识库类似文件夹，可包含多篇笔记；每篇笔记是一棵由根文档展开的知识树。
- 首页卡片代表最近打开或修改的笔记/知识树。
- 树为主、图为辅，但首版不考虑跨知识树关联。
- 生成入口依赖正文选区，首版内置“解释”和“翻译”。
- 生成开始时右侧出现占位节点；完成后保存为正式子文档；失败或取消则丢弃。
- 根文档、AI 子文档、手工文档与批注均可编辑。
- 拓扑默认覆盖当前根文档下全部节点，支持小窗浏览和放大铺满阅读区域。
- 数据需要本地存档，并可导出/导入压缩包。

## 原型方向
- 首页指定 `HTML-Demo/Index-White.html`。
- 阅读编辑器指定 `HTML-Demo/Demo-Note-Main.html`。
- 阅读原型包含：顶部面包屑与操作、左侧阅读路径、中部正文、右侧子文章、右下角可缩放/拖拽/固定拓扑、划词浮动工具条。

## 验证边界
- 先完成任务流与样式验证，再核对 Tauri 桌面构建。
- 当前记忆中的旧 Annota 实现来自过往 checkout；本轮目录为空，不能把旧能力当作当前已实现。

## 实现取舍
- 使用 React + TypeScript + Vite + Tauri 2；Tauri 窗口按 Windows 桌面设置最小尺寸与 NSIS 目标。
- 本地 demo 以版本化 React 状态 + `localStorage` 验证任务流，不宣称 SQLite、FTS、Credential Manager 或真实 LLM 已完成。
- 主页保留 `Index-White` 的纸白卡片、左侧索引和最近笔记结构；阅读器保留三栏、生成占位、路径与拓扑，但统一成冷白 Cobalt 设计语言。
- 阅读编辑器支持单块文本选择、原位 textarea、450 ms 自动保存、Enter 拆分与块首退格合并。
- `.annota` 当前是可演示 JSON 关系包，不是正式 v2 压缩格式。
- 为保证中文界面在不同 Windows 机器上保持一致，最终将 Noto Sans SC Variable 作为随应用打包的统一显示/正文/阅读字体，Cascadia/Consolas 仅用于短数据标签。

## 2026-07-26 窗口与动效修改
- Tauri 当前窗口仍为 `decorations: true`、1440×900；移除顶部原生标题栏需要改为无装饰窗口，并由前端提供拖拽区和三枚 Windows 窗口控件。
- `core:window:default` 只覆盖状态读取，不覆盖最小化、关闭、拖拽等写操作；能力文件需显式允许 `minimize`、`toggle-maximize`、`close`、`start-dragging`。
- Tauri 配置中的窗口宽高使用逻辑尺寸，WebView2 会随 Windows DPI 缩放；设置 1920×1080 即可维持系统缩放关联。
- 项目没有 motion 库；使用 CSS transform/opacity 和 React key 驱动的页面进入动画即可覆盖主页、阅读器及文章节点切换，并避免增加新依赖。
- 浏览器在 1920×1080 下实测：自定义标题栏为 40px，内容区为 1920×1040，文档 `scrollWidth` 与视口宽度均为 1920，无横向溢出；三枚窗口控件位于右上角，主页原有顶栏与内容比例保持正常。
- 首轮全屏拓扑验证发现路由动画终态的零位移 transform 仍会创建固定定位包含块，使面板多下移 40px；仅把终态写成 `transform: none` 仍会因 `animation-fill-mode: both` 保留插值矩阵，最终改为 `backwards`，让动画结束后回到基础样式的无 transform 状态。
- 修复后 1920×1080 全屏拓扑实测坐标为 top=102（40px 自定义标题栏 + 62px 阅读器工具栏）、left=246，路由容器 computed transform 为 `none`。
- 320/375/414/768 四档响应式实测均为 `scrollWidth === clientWidth`；320px 截图中标题栏控件完整显示、主页工具栏正确换行、按钮与正文无横向裁切，控制台无 error/warn。

## 2026-07-26 阅读页局部动效与三栏重排
- 当前文章 ID 被用于 `App.tsx` 的 `.route-stage` React key，导致每次切换文章都会重挂载整个 `ReaderPage`，顶栏、左侧路径、正文和右侧列表一起播放路由动画。
- 阅读路径与正文/子文章的布局目前只有两级：外层固定 `246px + 内容区`，内层再把正文与子文章作为居中的两列；因此子文章没有贴到工作区右缘，正文也不是在独立中央轨道内居中。
- 顶栏的 `.reader-breadcrumbs` 与左侧 `.reading-path` 重复表达同一条阅读路径；顶栏可直接移除该区，保留层级、类型、子文章数量等轻量元信息。
- 左侧宽度调整应使用真正的分隔拖拽控件，而不是只依赖 CSS `resize`：这样可以约束最小/最大值、支持键盘、保存用户宽度，并同步拓扑全屏面板的左侧偏移。
- 最终布局采用四轨工作区：`可调阅读路径 / 8px 分隔线 / 独立居中正文 / 右贴子文章`。1280×720 实测中路径为 246px、正文轨道为 666px、子文章为 360px，子文章右边缘等于 1280px。
- 父子文章切换实测：正文列分别为 `article-forward-in` 与 `article-back-in`；`.route-stage`、`.reader-topbar`、`.reading-path`、`.children-column` 的 animation-name 均为 `none`。
- 宽度调至 254px 后，正文轨道与全屏拓扑左缘均为 262px（254px 路径 + 8px 分隔线），证明拖拽状态与拓扑定位使用同一 CSS 变量。
- 当前轮应用内浏览器固定为 1280×720；安全策略禁止 `data:` 包装页进行 iframe 多宽度复验，因此没有声称重新目视验证 320/375/414/768。移动断点仍按 `≤900` 隐藏路径/分隔线、`≤768` 堆叠正文与子文章，并通过类型检查、生产构建和 Tauri 打包。

## 2026-07-26 启动路由、阅读区整合与拓扑尺寸
- `loadInitialData()` 会直接恢复本地数据中的 `currentNotebookId/currentArticleId`，因此应用重启后落在上次阅读页；启动加载应只保留内容数据与最后阅读节点记录，同时将两个当前路由字段归零。
- 子文章当前是 `.reader-workspace` 的第四条独立网格轨道；要满足 DOM 与视觉归属，应让 `.reader-workspace` 只保留路径、分隔线和阅读 `section`，再由 `section.reader-surface` 内部组织正文滚动轨道与右侧子文章。
- 拓扑展开尺寸固定为 `420×322`，拓扑视口固定为 `224px`；用户尺寸需要改成状态与 CSS 变量驱动，视口高度改为面板总高减去标题栏和页脚。
- 拓扑固定在右下角，真正会跟随鼠标的自由边是左边和上边；提供左边、上边和左上角三组拖拽区域，可避免右/下固定边产生反向拖动。
- 当前 `focusCurrent()` 会把缩放强制设置为窗口态 `0.9` 或全屏态 `1`；按 `F` 聚焦必须只用当前 `scale` 重算 `pan`，不调用 `setScale`。
- 1280×720 构建产物实测：`.reader-surface` 为 1026×618，正文轨道为 666×618，子文章列为 360×618 且右边缘等于 1280；子文章是阅读 section 的直接子级，文档横向溢出为 0。
- 拓扑宽度经边框控件从 420 调至 444 后，左边缘从 836 移至 812，标准 `ew-resize` 光标生效；PointerEvent 拖拽与 localStorage 持久化由 Vitest 覆盖。
- 拓扑缩放到 90% 后按 `F`，场景仅从 `translate(8px, 12px)` 平移到 `translate(115.3px, -27px)`，缩放仍为 0.9；当前节点中心与拓扑视口中心的坐标误差均小于 0.2px。
- `F` 只在拓扑全屏、固定、悬停展开或焦点位于拓扑内部时生效；折叠且未交互时不会意外打开面板。

## 2026-07-26 阅读画布视觉融合
- 用户截图显示当前割裂感来自两项叠加：正文轨道使用浅灰底，而右侧子文章列使用近白底和完整左边框，因此即使 DOM 已属于同一 `reader-surface`，视觉上仍像两个独立面板。
- `Demo-Note-Main.html` 的正文与子文章采用同一画布底色；右列通过较窄内容宽度、顶部对齐、标题分隔和卡片层级表达关系，不依赖整列背景或整高边框。
- 本轮设计方向保持 Workbench/Cobalt 冷白 token、Noto Sans SC 与既有功能顺序；阅读画布改为连续纸面，正文仍在独立阅读尺度内居中，子文章作为靠右的辅助内容列。
- 子文章卡片应降低“容器中的容器”感：去除常态阴影与重卡片边界，以透明/轻微混色表面和短促悬停反馈呈现；空状态同样不再像独立大框。
- 最终 1280×720 页面中，`reader-surface` 为 1026×618，正文轨道 666px、右侧子文章列 360px；右栏右边缘等于 1280px，左右背景连续且页面横向溢出为 0。
- 阅读画布成为统一滚动容器，正文不再生成位于两列之间的独立滚动条；滚动 420px 后右侧子文章列仍保持在阅读区顶部。
- 子文章列背景为透明、左边框为 0；空状态边框和圆角均为 0，以文字层级代替大号虚线框。
- 320/375/414/768 四个宽度均无根节点或 body 横向溢出；移动断点下正文与子文章改为单列，子文章取消 sticky 并自然接在正文下方。
- 主页当前由全宽 `window-titlebar`、全宽 `home-topbar`、再到 `home-workspace` 叠成三层；`home-topbar` 内的品牌按钮与窗口标题栏中的 Annota 品牌重复。
- 本轮采用清晰的两级层次：`window-titlebar` 继续负责全局品牌与 Windows 窗口控制；`home-topbar` 只负责主页搜索、工作区状态和设置，并与 `home-main` 一起放入右侧 `home-content`。
- 1280×720 实测中，`window-titlebar` 为全宽 1280×40；其下侧栏从 x=0 开始，右侧 `home-content` 从 x=236 开始。`home-topbar` 为 1044×66，底边 y=106，`home-main` 顶边同为 y=106，二者无间隙且不存在重复品牌按钮。
- 320/375/414/768 宽度下侧栏均隐藏，`home-topbar` 与 `home-main` 仍前后紧邻，根节点与 body 横向溢出均为 0；1280×720 控制台无 warning/error。

## 2026-07-26 编辑器空行与格式栏交互
- 空段落保留的直接原因是 `BlockEditor` 只有输入、拆分、合并和自动保存逻辑，没有在活动段落变化或编辑器失焦时压缩空块。
- `contenteditable` 使用单一根焦点；仅监听根节点 `blur` 无法覆盖用户从空行点击到同一编辑器其他段落的场景，因此需要跟踪活动块 ID，并在块间指针切换时清理离开的空块。
- 颜色按钮原本只切换菜单，真正的格式命令只由色块选项触发；拆分为“应用最近颜色”主按钮和独立下拉按钮后，常用操作只需一次点击。
- 初版仅把正文网格改为 `1fr`，但 1280×720 下正文列可用宽度只有约 581px，工具栏仍与正文等宽；实测证明工具栏必须成为 `reader-surface` 的直接子级并跨越两列。
- 最终 1280×720 实测：`reader-surface` 为 1026×618，工具栏可用宽度 1018px（扣除滚动条），正文为 581.2px 且最大宽度仍为 760px，子文章列 360px，页面横向溢出为 0，控制台无 warning/error。

## 2026-07-27 AI 模型服务配置界面
- 用户参考图的可迁移结构是左侧服务商列表、右侧当前服务商接口表单，不沿用参考图的会员开关、翻译专属文案或深色主题。
- Annota 当前设置工作区外层已有 210px 分类侧栏，因此模型服务内部列表应保持紧凑，桌面端约 220–240px，并在窄屏改为横向服务商选择带。
- 本轮配置范围限定为服务商名称、接口协议、Base URL、API Key、默认模型与启用/默认状态；提示词和上下文不在此处编辑。
- 最终请求边界应在界面中明确表达为“服务商配置 + 提示词模板 + 当前上下文 → 请求 JSON”，但本轮不接真实请求与凭据存储。
- 当前模型分类仍由通用 `groups/items` 生成空白槽位，需要在渲染层为 `models` 提供专用面板，避免把可配置表单强行塞进通用设置行。
- 当前样式文件中的设置分类侧栏仍是桌面 `clamp(240px, 20vw, 280px)`、900px 以下 `220px`；实现本轮布局时应恢复用户已要求的约 210px 宽度。
- 最终桌面预览采用 210px 设置分类侧栏、232px 模型服务列表与弹性接口配置区；1272px 视口下配置区约 505.9px，根节点 `scrollWidth === clientWidth`，无横向溢出。
- 视觉自检确认双栏结构已复用参考图的信息层级，但保留 Annota 的冷白 Cobalt 主题；调用 JSON 组装链是唯一高强调区域，其余表单保持克制。
## 2026-07-27 内容显示风格
- `内容显示.md` 定义的是五套界面内容术语，不是颜色主题：传统、数字花园、认知神经、星际图谱、学术策展。
- 九类可切换术语为主页、文件夹、标签、知识图谱、最近浏览、收藏、新建文章、子文章、划词生成。
- 风格切换需要即时影响实际界面文案；仅展示设置卡片不足以满足“切换为其他风格显示”。
- 默认值应为传统模式，并通过独立本地偏好键保存，避免混入字体偏好或应用内容数据。
- 当前九类术语主要分散在 `App.tsx` 的全局搜索、`HomePage.tsx` 的侧栏/主页操作/最近区域，以及 `ReaderPage.tsx` 的返回主页、子文章和生成动作。
- 最小稳定实现是在 `App` 持有显示风格，向主页、设置页和阅读器传递同一份术语映射；内容数据中的文章类型不应被改写。
- 设置切换器应放在通用设置组之后，使用 radio 语义；桌面为左侧五个风格、右侧九项术语预览，窄屏降为单列。
- 用户追加要求：左侧需要 `+` 自定义入口；选中后右侧从只读预览切换为风格名称与九项术语输入框，修改即时作用于界面并单独持久化。
- 桌面真实页面已确认默认传统模式、五个预设与 `+` 自定义入口均具有正确 radio 语义；设置区域位于“外观与交互”底部，左侧选择、右侧术语预览符合要求。
- 1280×720 自定义编辑态实测：左右列约为 239px / 425px，共 10 个输入框（名称 + 九项术语）全部位于视口内，页面横向溢出为 0。
## 2026-07-27 快捷键管理
- 用户要求独立快捷键设置页不仅列出快捷键，还必须允许修改，并让修改实际影响应用行为。
- 实现需要把分散在组件内的硬编码判断集中为可持久化配置；仅修改设置页显示会造成界面与行为不一致。
- 当前可自定义的应用命令共六项：打开全局搜索 `Ctrl+K`、保存当前文章 `Ctrl+S`、返回父文章 `Alt+Left`、打开/关闭拓扑 `Ctrl+E`、回到根文章 `Ctrl+Home`、聚焦拓扑当前节点 `F`。
- `Enter/Backspace` 编辑语义、`Escape` 关闭菜单/弹窗、分隔条方向键属于控件标准键盘行为，不应与应用命令注册表混合或允许重映射。
- 配置应由 `App` 统一持有并向 Home/Reader/BlockEditor/Topology 传递；设置页使用聚焦按钮录入下一组组合键，必须拒绝修饰键单独录入和重复绑定。
- 设置页最终按“全局 / 阅读 / 拓扑”三组呈现六项命令；录入按钮保持稳定宽度，以 `aria-pressed` 表示正在录入，并用统一格式显示为 `Ctrl + E`。
- 聚焦回归将修改“打开或关闭拓扑”为 `Ctrl+G`，检查本地配置后离开设置并验证 `Ctrl+G` 真实打开全屏拓扑。
- 1280×720 真实页面可访问性快照确认：快捷键分类展示“全局 / 阅读 / 拓扑”三组、六项命令，默认键位为 `Ctrl+K`、`Ctrl+S`、`Alt+Left`、`Ctrl+Home`、`Ctrl+E` 与 `F`。
- 1280×720 几何测量结果：文档横向溢出为 0，三组、六行、六个录制按钮均未越界；录制按钮固定为 150px，进入录制后 `aria-pressed=true` 且文案切换为“请按下新组合键”。

## 2026-07-27 模型列表发现与 Zhipu 品牌
- 用户希望默认模型不再只能填写字符串，而是能从服务商模型列表接口加载后下拉选择，同时保留接口不可用时的手动输入能力。
- GLM 服务商的正确 LobeHub 品牌名称为 `Zhipu`，要求使用品牌色 `#3859FF`。
- 当前计划沿用现有冷白 Cobalt 设置界面，只在模型字段附近增加明确的“获取模型”动作、加载/错误/空状态与手动兜底，不重做整个模型服务页面。
- 当前 `ModelProvider` 仅保存 `baseUrl`、请求路径、API Key 和单个 `model` 字符串，没有模型列表 URL、加载状态或候选模型集合。
- `@lobehub/icons` 当前版本已导出 `Zhipu`，现有错误来自组件仍导入并渲染 `ChatGLM`；可直接替换为 `Zhipu`，不需要新增依赖。
- 模型字段目前位于“接口定义”的双列网格中；适合升级为稳定的组合控件：模型列表下拉、刷新动作与“手动填写模型 ID”切换，避免自动发现失败时阻断配置。
- 当前桌面端只有 `list_system_fonts` Tauri command，没有通用 HTTP IPC；如果只在 WebView 前端直接 `fetch`，第三方服务商可能因 CORS 阻止模型列表请求。
- 稳定方案是在 Tauri Rust 侧新增模型发现命令，通过 `reqwest` 请求用户填写的模型列表 URL；浏览器开发模式保留同结构的 `fetch` 实现，便于本地预览和聚焦交互。
- OpenAI Compatible、Anthropic 与多数兼容服务的模型响应都可归一化为模型 ID 列表；请求默认使用 `Base URL + /models`，同时允许模型列表字段填写完整 URL。
- Zhipu 图标模块自带 `Color` 变体，包内 `COLOR_PRIMARY` 正是 `#3859FF`；使用 `Zhipu.Color` 可同时满足正确图形、品牌色与统一白底图标容器。
- 模型发现控件应保持用户当前填写的模型不被后台列表自动覆盖；加载成功后切换为下拉选择，如果当前值不在返回列表中则作为“当前填写”选项保留。
- 页面顶部原“不会发起网络请求”的说明需要同步改为：仅点击获取模型时请求模型列表，真实生成请求仍未接入，API Key 仍只是页面临时状态。
- 现有设置页聚焦用例已经覆盖服务商切换和字段显示，可在同一段中用浏览器开发回退的 `fetch` 模拟模型响应，验证“获取模型 → 下拉选择 → 请求 JSON 预览更新”的完整前端链。
- 全局 `prefers-reduced-motion` 规则会把新增刷新图标动画压缩到 0.01ms，因此不需要额外增加单独的降级选择器。
- 1280×720 真实页面确认模型服务表单完整呈现模型列表地址、获取动作与手动默认模型；页面横向溢出为 0，模型发现组合控件宽 428px 且未越界。
- GLM 详情中的新品牌 SVG 已实际渲染为 Zhipu 路径，路径填充为 `#3859FF`，外层品牌标记背景为纯白 `rgb(255, 255, 255)`。
- 模型目录失效时必须强制回到手动输入；否则用户加载过目录后再修改 Base URL/API Key，会因旧的“使用列表”模式留下一个空下拉框。最终判定已改为“目录为空时始终手动输入”。

## 2026-07-28 使用模型下拉修复
- 用户明确不要“先点获取模型、再切换选择框”的两阶段操作；目标是 `default-model-chatgpt` 所在位置直接显示“使用模型”，点击字段右侧箭头即展开并自动访问模型 URL。
- 下拉浮层需要在模型返回前保持打开，直接显示加载状态；成功后就地显示选项，失败时显示可理解的原因和重试入口。
- 之前的模型 URL 输入仍应保留，因为它决定箭头展开时访问的地址；独立“获取模型”按钮应从 URL 行移除，避免两套入口竞争。
- 官方接口核对确认 OpenAI 使用 `GET https://api.openai.com/v1/models` 与 Bearer API Key；Gemini OpenAI Compatible 使用 `GET https://generativelanguage.googleapis.com/v1beta/openai/models`，同样使用 Bearer API Key。
- DeepSeek 官方文档确认当前 Base URL `https://api.deepseek.com` 下提供 `GET /models`，响应为 OpenAI 风格的 `data[].id`。
- 不是所有服务商文档都公开统一的动态模型列表端点，因此组合选择器不能假设请求必然成功；失败状态必须允许重试并直接进入手动模型 ID。
- Anthropic 官方 API 也提供 List Models 资源；现有 `x-api-key + anthropic-version` 请求方式可继续用于 Claude 服务。
- 当前问题的直接结构原因是模型发现与模型选择被拆成两个互相切换的控件：URL 行的“获取模型”按钮负责请求，模型行先显示输入框、成功后才变为原生 select，用户第一次点击模型字段不会发生发现。
- 修复后应始终保留一个稳定的 `default-model-<provider>` 组合框触发器；箭头展开浮层时发起请求，浮层内部承载筛选、模型选项、刷新、错误和自定义 ID，不再改变主字段的控件类型。
- 响应解析还应识别 Gemini 原生模型对象的 `baseModelId`，并在只收到 `name: models/...` 时去掉 `models/` 前缀，避免把资源路径误当成生成模型 ID。
- 新组合选择器的主触发器始终保持 `id=default-model-<provider>` 和 `role=combobox`，当前模型与右侧箭头位于同一 42px 控件内；浮层使用独立 listbox，不再依赖异步替换原生 input/select。
- 为避免用户修改 URL/API Key 后旧请求晚到并覆盖新状态，模型请求需要按服务商记录递增请求编号；重置配置会使旧响应失效。
- 1280×720 成功响应实测确认：箭头展开后显示 6 个返回模型，选择 `gpt-5.1` 后组合框关闭并更新文本，页面横向溢出为 0，420px 浮层左右边界均在视口内。
- 初版浮层在该视口下底边为 y=728，超出 720px 视口 8px；应缩短模型列表最大高度并启用内部滚动，使状态、搜索、选项和自定义入口无需页面额外滚动即可完整操作。
- 将模型列表最大高度改为 188px 后，1280×720 实测浮层边界为 left=779、right=1199、top=389、bottom=690，完全位于视口内；列表自身可滚动，页面横向溢出仍为 0。
- 网络不可达实测时浮层保持展开，显示“无法访问模型列表，请检查地址、网络连接或浏览器跨域限制”，搜索/自定义 ID 输入仍可操作，避免请求失败后形成无出口状态。

## 2026-07-28 内置模型目录与窄窗侧栏
- 用户要求内置目录是无 Key 时的主要选择来源；有 Key 时才进行云端目录请求，因此打开组合框不应再无条件发起网络访问。
- 内置目录需按服务商维护；云端成功返回后只呈现账号实际可用项，云端为空或失败时再回退内置目录。
- 默认服务商的接口地址属于应用预设，不应允许误改；自定义服务商仍保留完整可编辑能力。
- 小窗口侧栏应保留稳定的左侧命中热区，展开动画作用于侧栏本体，避免触发区随动画移动而产生悬停抖动。
- 覆盖式侧栏不能把展开状态绑定到任何点击后可能保留的焦点伪类，否则会持续遮挡右侧内容；本轮按用户要求仅用稳定外层热区的 `:hover` 展开。
- OpenAI 官方当前模型页将 GPT-5.6 Sol/Terra/Luna、GPT-5.5/Pro 与 GPT-5.4/Pro/mini/nano列为前沿模型；内置目录只纳入文本/代码生成模型，不混入音频、图像、嵌入或已弃用型号。
- Gemini 官方当前页的通用生成系列为 Gemini 3.6 Flash、3.5 Flash、3.5 Flash-Lite、3.1 Flash-Lite、3.1 Pro Preview，以及仍可用的 2.5 Pro/Flash/Flash-Lite；不纳入 Live、TTS、图像、视频、嵌入等专用端点型号。
- Kimi 官方模型页当前列出 `kimi-k3`、`kimi-k2.7-code`、`kimi-k2.7-code-highspeed`、`kimi-k2.6`、`kimi-k2.5` 与 Moonshot V1 系列；其中 K2.5/Moonshot V1 已不对新用户开放并计划于 2026-08-31 全面下线，因此内置目录仅保留新一代 K3、K2.7 Code 和 K2.6。
- DeepSeek 官方当前模型列表只有 `deepseek-v4-flash` 与 `deepseek-v4-pro`，旧 `deepseek-chat`/`deepseek-reasoner` 已于 2026-07-24 停用，不纳入内置目录。
- Anthropic 官方最新对比表的通用可用型号为 `claude-fable-5`、`claude-opus-4-8`、`claude-sonnet-5` 与 `claude-haiku-4-5-20251001`；受邀的 Mythos 型号不纳入普通内置目录。
- 智谱官方文本模型概览当前列出 GLM-5.2、5.1、5、5-Turbo、4.7、4.7-FlashX、4.6、4.5-Air/AirX、4-Long、4-FlashX-250414、4.7-Flash 与 4-Flash-250414；明确“即将下线”的 GLM-4.5-Flash 不纳入目录。

## 2026-07-28 主页与最近浏览滚动切页
- 当前 `home-content` 已天然分成 66px 顶栏与剩余内容区，适合保持顶栏不动，只在第二行内部放置两个等高页面并做纵向位移。
- 当前最近浏览与首页概览共用一个 `.home-main` 滚动区；需要拆为概览页和最近浏览页，后者拥有独立滚动容器，才能满足“滚到最近浏览顶部后再上滑才返回”的边界。
- 桌面交互采用滚轮边界切换：首页接收到向下滚动即进入最近浏览；最近浏览仅在 `scrollTop <= 0` 且继续向上滚动时返回。触摸滑动与键盘 PageUp/PageDown 作为同语义补充。
- 最近浏览必须固定按 `updatedAt` 降序，不再提供“按连接”切换；按本地日期分组后，左侧日期/时间与连续竖线构成真实时间线，右侧继续复用 `.notebook-card`。
- `15_Cpp` 中的“虚函数与动态多态”“虚函数表”“虚析构函数”“模板基础与函数模板”等文件包含 YAML 标签、摘要和正文，适合作为真实 C++ 演示数据来源；界面数据只复制必要标题、摘要与核心段落，不依赖运行时访问外部目录。
- 真实预览发现旧版本 `annota.desktop.demo.v1` 本地缓存会优先于新 seed 加载，导致升级后仍只显示四篇旧示例；加载时需要按固定 ID 只补缺失的 C++ 内置文章与主笔记，同时保留用户已有内容和同 ID 修改。
- 1280×720 实测：首页概览完整位于顶栏下方；最近浏览切入后独立滚动区为 614px 高、内容高 1324px，页面横向溢出为 0，时间线日期、圆点、竖线与卡片列对齐。

## 2026-07-28 主页信息层级与悬浮操作入口
- `home-topbar` 只承载全局搜索和本地状态；移除后 `home-content` 可直接使用单行满高布局，不影响设置页。
- 主页概览目前只有标题、概况、规划中提示与下滑按钮，没有任何最近浏览卡片；可复用现有 `NotebookCard` 展示时间倒序的前三项，并继续保留完整时间线作为下一屏。
- 全局搜索最合适的位置是概况条之后、最近浏览预览之前，既接近原 `planned-ai` 占位，又符合“标题之后、笔记内容之前”的顺序。
- 新建与导入可用原生 `details/summary` 组成右下角操作坞：鼠标悬停和键盘聚焦显示菜单，点击加号也能稳定打开，避免只依赖 hover。
- 下滑提示与底边距离偏大来自概览页继承了为悬浮按钮预留的 `104px` 底部内边距；概览页可单独覆盖为 8px，让提示真正贴近页面底部。
- 原生 `details` 的 `open` 状态会在鼠标移出后继续保留，不符合“移开后归位”；改用普通悬浮容器加受控展开状态，可在整个按钮/菜单区域触发进入与离开时准确收放。
- 主页概况与搜索框需要共同父级才能稳定按比例横排；使用 `1.15fr / 0.85fr` 后，搜索框占可分配行宽的 42.5%。
- 绝对定位菜单与圆形触发按钮之间的视觉间距不属于父元素常规盒模型，向上移动鼠标时可能触发提前收起；菜单下方的透明伪元素可作为连续悬浮连接区。
- 仅让菜单容器上移不够明显；选项从 `translateY(14px)` 开始并按距离触发按钮的顺序错峰出现，更符合“向上弹出”的方向感。
- `Notebook` 目前没有显式收藏字段；收藏页若不扩展数据模型，应采用稳定的演示集合并在界面文案中避免暗示真实持久化收藏状态。
- `NotebookCard` 当前定义在 `HomePage.tsx` 内部，独立资料页面应通过 `renderNotebook` 渲染回调复用卡片，避免反向导入 `HomePage` 形成循环依赖。
- 主页侧栏在 1180px 以下使用 56px 收起轨道、悬停扩展到 236px，新增导航必须继续使用 `.home-nav-item` 才能继承图标收起与文字展开行为。
- 新资料视图应替代主页双页视口，而不是塞进 `activePage` 的 overview/recent 状态；这样文件夹、标签、收藏不会触发主页滚轮切页逻辑。
- 桌面侧栏在 768px 以下会完全隐藏，因此新增资料视图同时需要一个仅窄屏显示的四项顶部页签，才能保持主页、文件夹、标签与收藏均可访问。
- 文件夹直接按 `Notebook.category` 聚合，标签按 `Notebook.tags` 聚合；分类字符串只作为标签展示，不按 `/` 拆成真实目录层级。
- 悬浮新建/导入入口继续只显示在主页视图，避免覆盖资料页内容并保持既有交互边界。
- 文件夹、标签与收藏属于资料浏览工作区，不应设置内容最大宽度；保留页面内边距的同时让 `.library-view` 使用 100% 可用宽度，可使分组卡片自然扩展到整个内容区域。
- 文件夹的核心语义是内容集合，因此初始页应展示集合级卡片，而不是直接平铺每个集合内部的笔记卡片；卡片需要同时呈现笔记数、页面数与部分内容标题。
- 文件夹打开态最适合用独立的 `activeFolderKey` 管理，不复用主页或资料页一级导航状态；这样左侧栏可以快速切换集合，返回按钮也只重置文件夹内部层级。
- 打开态的左侧栏在桌面保持竖向 sticky 索引，右侧复用完整笔记卡片；768px 以下改为横向可滚动索引，避免固定窄栏挤压移动内容区。

## 2026-07-28 可定制文件夹集合与属性编辑
- 用户把文件夹定义为带领域属性的笔记集合；名称、颜色、图标和“文件夹归类”都属于集合元数据，不应继续完全等同于 `Notebook.category` 文本。
- 当前应用还没有独立持久化文件夹实体；`AppStore` 已持久化完整 `AppData`，因此可以增加可选 `folderProfiles` 字段，并为旧缓存从 `Notebook.category` 生成默认属性，既不破坏既有笔记数据，又能让编辑结果跨页面保留。
- 文件夹卡片主体应直接作为进入集合的按钮语义；属性编辑必须是独立图标按钮并阻止冒泡，避免“进入文件夹”和“编辑属性”竞争。
- 横向矩形卡片适合使用约 1.7:1 的比例：左侧颜色/图标锚点，中间集合摘要与内容标题，右下角显示最近打开笔记；无需“查看文件夹”文案。
- “文件夹归类”类似文件夹专用标签，应作为可编辑的字符串集合展示在卡片和详情头部，并与笔记自身标签保持概念分离。
- 动效集中在列表错峰进入、卡片轻微抬升、详情滑入和编辑弹层缩放淡入；全局 reduced-motion 规则继续作为降级边界。
- 具体场景定为“管理 C++ 与底层知识笔记的个人知识工作台”，文件夹页的单一任务是识别集合、判断最近进度并进入或编辑它。
- 视觉 token 继续复用现有冷白纸面、石墨文字、Cobalt 交互色与文件夹自定义色；自定义色只出现在集合书脊、图标底板和状态细节，不改变整个页面主题。
- 字体继续沿用现有 display / body / mono 三角色：集合名称用 display，摘要与内容标题用 body，数量、时间和归类用 mono。
- 布局采用横向“档案夹索引卡”：彩色书脊与图标在左，集合摘要在中，最近打开笔记作为底部横向记录条；这比通用的图标 + 数字 + 按钮仪表盘卡更贴合文件夹集合语义。
- 记忆点是每张卡片的“自定义色书脊 + 最近打开记录条”；其余边框、阴影和文字保持克制，避免多个视觉亮点相互竞争。

## 2026-07-28 文件夹 CRUD、批量管理与图标库
- `lucide-react` 已是项目依赖，当前文件夹编辑页只暴露其中 8 个图标；可以直接扩展选项和搜索，不需要引入新的包或网络资源。
- 新建空文件夹要求集合列表从“仅按笔记分类聚合”改成“文件夹属性与笔记分类的并集”，否则没有笔记的新文件夹无法显示。
- 文件夹稳定键与显示名称必须继续分离；新文件夹使用生成键，改名只更新显示属性，避免破坏未来的笔记归属关系。
- 批量删除不能级联删除内部笔记；安全语义是删除文件夹属性，并把匹配这些稳定键的笔记分类改为 `未归档`。
- 文件夹查询应覆盖名称、说明、专属归类和内部笔记标题，作为 CRUD 中“查”的直接入口。
- 批量模式使用显式复选框，不改变常态下点击卡片直接打开集合的行为。
- 当前 reducer 只有单个 profile upsert，需增加批量 upsert 与批量删除动作；后者同时重写匹配文件夹键的 `Notebook.category`。
- 当前 `folderCollections` 完全由笔记分组映射而来，即使 `folderProfiles` 中存在空集合也不会渲染；必须改为先遍历 profiles，再补没有 profile 的笔记分组。
- 当前属性弹层可以直接承担新建流程，只需增加 create/edit 模式、生成稳定键与不同的标题/提交文案，避免维护第二套表单。
- 轻量审查发现旧缓存迁移逻辑会自动补回缺失的 seed 文件夹属性；如果只从 `folderProfiles` 删除，内置文件夹重启后会重新出现。需要保存 `deletedFolderKeys` 墓碑并让迁移逻辑尊重它，才能让删除真正持久化。

## 2026-07-28 文件夹新建按钮悬浮定位
- 当前桌面壳无需为右下角文件夹按钮使用 `document.body` Portal；组件内固定定位更稳定，也能直接继承现有样式与生命周期。
- Windows 桌面 WebView 的主要目标不需要安全区表达式参与基础定位；直接的 `right`/`bottom` 间距是更可靠的默认值。
- 应用标题栏使用 `z-index: 300`，资料页主操作按钮采用 `z-index: 320` 可以确保其不被应用内容层覆盖，同时按钮位于右下角，不会与标题栏发生几何重叠。

## 2026-07-29 文件夹卡片操作语义
- 文件夹卡片的“打开集合”和“编辑属性”可以通过同一卡片中的两个兄弟按钮实现：主按钮覆盖卡片内容，图标编辑按钮以更高层级叠放在书脊位置，避免嵌套按钮产生无效 HTML。
- 批量模式下不渲染图标编辑按钮，让主按钮完整接管整张卡片；右上角选中标记设置 `pointer-events: none`，只反馈状态而不缩小点击范围。
- 单项删除入口移除后，安全删除仍可由属性编辑页和批量工具栏承担，因此不需要删除 store 中已有的删除能力。

## 2026-07-29 文件夹编辑页布局与图标选择
- 当前 `folder-editor-body` 是“左侧全部表单 + 右侧预览”；目标结构可直接改为“左侧名称/颜色/图标/预览 + 右侧归类/说明”，无需变更表单状态或保存逻辑。
- 图标按钮已有完整 `aria-label`，移除可见名称后仍具备无障碍名称；补充 `title` 可为鼠标用户保留名称提示。
- 左侧约 440px 可用宽度适合以 42px 为最小单元的自适应图标网格，20 个图标通常可以在两到三行内完整展示。
- 视觉继续沿用冷白纸面、Cobalt 选中态与现有 display/body/mono 字体；本轮唯一强化点是紧凑的“图标样本盘”，其余表单保持克制。
