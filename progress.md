# Progress

## 2026-07-26
- 已检查外层目录与 `project/`：确认从空目录开始。
- 已读取 `planning-with-files` 与 `frontend-design` 技能说明。
- 已开始读取 `hallmark` 规范、产品文档和两个 HTML 原型。
- 已建立持久化计划、发现记录和进度日志。
- 已完成需求/原型结构化提取。
- 已建立 React/Vite/Tauri 2 工程、Cobalt token 系统和 Windows 窗口配置。
- 已完成主页、全局搜索、导入/新建、阅读路径、块编辑、选区生成、子文章、拓扑和本地持久化。
- 已添加核心流程测试与 README。
- `npm install` 成功，安装 190 个包；npm 报告 5 个依赖审计项，尚未执行破坏性 `audit fix --force`。
- 首轮验证发现 Vitest `spawn EPERM` 沙箱限制与一个 tsconfig 配置冲突；已修复 tsconfig。
- `npm run check` 与 `npm run build` 已通过；沙箱外 Vitest 2 项核心流程测试已通过。
- Tauri 首轮检查发现缺少 Windows ICO；已由项目 SVG 生成平台图标，`cargo check` 随后通过。
- 浏览器已在 1440×900 验证主页、阅读器与全屏拓扑；320/375/414/768 四个宽度均无横向溢出，控制台无错误。
- 已补充并通过单块选区与生成占位的自动化测试；Vitest 共 3 项通过。
- 最终 `npm run check`、`npm run build`、`cargo check` 全部通过；生产依赖 `npm audit --omit=dev` 为 0 漏洞。
- 最终字体改为随包分发的 Noto Sans SC Variable，并重新验证桌面/响应式布局与控制台。
- Hallmark 预发自评 P5/H4/E4/S5/R5/V4；58 项反模板化检查通过。
- `npm run tauri build` 成功，生成：
  - `src-tauri/target/release/annota.exe`
  - `src-tauri/target/release/bundle/nsis/Annota_0.1.0_x64-setup.exe`
- NSIS 安装包 SHA256：`C9B2833B403CE976FE09E6C76058A8C1C154E859A0BB8871CE8BA31DAEF8B217`。
- 开始 Phase 6：确认窗口配置、Tauri capability、页面切换入口与现有 Hallmark/Cobalt 动效约束。
- Phase 6 第一轮 `npm run check`、`npm run build`、`cargo check` 通过；Vitest 在沙箱内触发已知 `spawn EPERM`，待授权环境复验。
- 授权环境下 Vitest 3/3 通过；1920×1080 浏览器预览确认自定义标题栏、主页布局与页面进入动画正常且无横向溢出。
- 1920×1080 阅读器、子文章前进、父文章返回、拓扑全屏均已实测；修复路由 transform 终态造成的全屏拓扑 40px 坐标偏移。
- 复验确认全屏拓扑 top=102 定位正确；320/375/414/768 响应式无横向溢出，浏览器控制台 0 error/warn。
- Phase 6 最终 `npm run check`、`npm run build`、Vitest 3/3、Tauri release/NSIS 构建全部通过。
- 新安装包：`src-tauri/target/release/bundle/nsis/Annota_0.1.0_x64-setup.exe`（6,486,321 bytes）。
- 新安装包 SHA256：`7D532FBCB948D59412E60FE3FBBC4ADB562361C865BC7DF503808521C6A5974C`。
- Hallmark 最终自评 P5/H4/E5/S5/R5/V4；58 项检查通过，动效仅使用 transform/opacity 且包含 reduced-motion 降级。
- 开始 Phase 7：定位到应用级 `.route-stage` 动画、重复顶栏路径、固定 246px 左栏与内层居中双列是三项需求的直接实现点。
- 确定改为“可调宽左侧路径 + 独立居中正文 + 右贴子文章”的工作区四轨布局，并将文章切换 key/动效下沉到正文列。
- 已完成应用级路由动画移除、正文局部前进/返回动效、顶栏重复路径移除、可调宽路径分隔线和右贴子文章布局；`npm run check` 与 `npm run build` 通过。
- Vitest 首次复验遇到已知 Windows 沙箱 `spawn EPERM`，已记录并转到允许环境复验。
- 沙箱外测试暴露两个测试环境问题：示例子文章标题断言不匹配、jsdom 缺少 Pointer Capture API；均已针对性修正，未改变浏览器中的拖拽行为。
- 4 项 Vitest 全部通过；拖拽测试同时覆盖方向键调宽与指针从 254px 拖至 326px 的持久化。
- 应用内浏览器在 1280×720 复验：正文位于独立中央轨道，子文章右边缘等于视口右边缘；顶栏无阅读路径；前进/返回时仅正文列有动画，其他区域动画均为 `none`。
- 路径宽度从 246px 调至 254px 后，正文轨道左缘和全屏拓扑左缘同步从 254px 调至 262px；浏览器控制台无 error/warn。
- 最终 `npm run check`、`npm run build`、Vitest 4/4 与 `npm run tauri -- build` 全部通过。
- 新 Windows 可执行文件：`src-tauri/target/release/annota.exe`（13,171,200 bytes，SHA256 `7D4B6ED858843CD152808671E2D5FCC7E10CBF6E389F31A3D8B22CEE86EDDB7F`）。
- 新 NSIS 安装包：`src-tauri/target/release/bundle/nsis/Annota_0.1.0_x64-setup.exe`（6,487,969 bytes，SHA256 `7FB4F04331EED03159D2E421EBCF8402D26E755C14853F49AAE4D95FE10F9911`）。
- Phase 7 完成；Hallmark 最终自评 P5/H5/E5/S5/R5/V4。当前轮真实浏览器桌面验证通过，移动宽度因浏览器安全策略仅完成代码/构建复核。
- 开始 Phase 8：确认启动路由恢复、子文章独立网格轨道、拓扑固定尺寸和聚焦时强制缩放是四项需求的直接实现点。
- 确定保持现有 Workbench/Cobalt 系统，只修改 `AppStore`、阅读器、拓扑组件、样式和自动化测试；不删除生产文件。
- 已实现默认主页启动、子文章并入阅读 section、拓扑边框缩放与尺寸持久化、`F` 保持缩放聚焦当前节点；类型检查与生产构建首轮通过。
- Phase 8 首次 Vitest 被已知 Windows 沙箱 `spawn EPERM` 阻断，尚未进入测试断言，转到允许环境复验。
- 沙箱外 Vitest 的 6 项断言通过，但因阅读器旧键盘监听器未守卫 `window` 事件目标而报告 1 个未处理异常；已定位到 `ReaderPage.tsx` 并补充 HTMLElement 类型守卫。
- 修复后 `npm run check` 与 Vitest 6/6 通过，无未处理异常。
- 视觉验证服务器首轮 `Start-Process` 被重复 `Path/PATH` 环境键阻断，未产生监听进程；准备以干净子进程环境重试。
- `-UseNewEnvironment` 未绕过 PowerShell 的重复键缺陷；第二次仍未启动进程，已切换到不同的 Windows 进程创建接口。
- Windows `Win32_Process.Create` 被权限策略拒绝，第三种后台启动方案仍未产生监听；不再重试系统进程创建，转为托管已验证构建产物。
- 受控静态服务上的 1280×720 页面验证通过：重新打开默认落在主页；正文与子文章同属阅读 section；右栏贴边且无横向溢出。
- 拓扑真实页面宽度调整从 420px 到 444px；缩放到 90% 后按 `F`，当前节点中心误差小于 0.2px，缩放保持 90%。
- 最终 `npm run check`、Vitest 6/6、`npm run build` 与 `npm run tauri -- build` 全部通过；Tauri 仅输出非阻断的 Windows 导入库链接器提示。
- 最终 Windows 可执行文件：`src-tauri/target/release/annota.exe`，13,172,224 bytes，SHA256 `B0FB38F4DFD92CFCA585155C3ECDB2634F3EA561637F2447AEE98C47C8416474`。
- 最终 NSIS 安装包：`src-tauri/target/release/bundle/nsis/Annota_0.1.0_x64-setup.exe`，6,489,579 bytes，SHA256 `88628D5B06B70F3B6B762AA908588F7861F58F0EADFD63D447CA7FB8C5B591C9`。
- Phase 8 完成；Hallmark 自评 P5/H5/E5/S5/R5/V4，未引入 transition-all、原始颜色/字体值或布局属性过渡。
- 开始 Phase 9：对照两张用户截图与 `Demo-Note-Main.html`，确认阅读区割裂主要由左右不同底色、右列整高边框和重卡片表面造成。
- 本轮限定修改阅读区 CSS 与规划记录，不变更文章数据、拓扑交互或路由结构。
- 已将右栏标题改为“当前文章标题 的子文章 + 数量”，移除卡片尾部多余箭头；可访问名称继续保留“下一级子文章”。
- 已统一正文与子文章背景，移除右栏整高边界与常态阴影，空状态改为无框左对齐提示。
- 已把滚动容器上移至整个 `reader-surface`，右侧子文章列在桌面端 sticky；1280×720 下右栏 360px 并准确贴靠右边缘。
- 最终 `npm run check`、Vitest 6/6、`npm run build` 与 `npm run tauri -- build` 全部通过；Tauri 仅输出既有的 Windows 导入库链接提示。
- 浏览器实测覆盖 1280×720、320、375、414、768；各尺寸横向溢出为 0，控制台无 error/warn。
- Phase 9 完成；Hallmark 自评 P5/H5/E5/S5/R5/V4，未新增 `transition-all`、原始颜色/字体值或布局属性过渡。
- 最新 Windows 可执行文件：`src-tauri/target/release/annota.exe`（13,172,224 bytes，SHA256 `032C90B2EB402105DB824D962307070D9E8ACCE5FD38F6DBF24A733F8BEC731A`）。
- 最新 NSIS 安装包：`src-tauri/target/release/bundle/nsis/Annota_0.1.0_x64-setup.exe`（6,489,199 bytes，SHA256 `3E11F104F347F5C6FF3C19B52A1C4D1D95A30B4BF4E7D34E5A90D6EE578DC66C`）。
- 开始 Phase 10：确认主页的重复顶栏来自 `window-titlebar` 与横跨全宽的 `home-topbar`；将保留前者为应用级标题栏，并把去掉重复品牌入口后的主页工具栏移动到 `home-main` 正上方。
- 已完成主页 DOM 与网格结构调整，移除重复品牌按钮；`npm run check` 通过，Vitest 首次运行被 Windows 沙箱 `spawn EPERM` 阻断。
- 沙箱外 Vitest 6/6 通过，`npm run build` 通过。
- 应用内浏览器已验证 1280×720 主页的两级标题栏层次，以及 320/375/414/768 响应式布局；各宽度无横向溢出，浏览器控制台无 warning/error。
- Phase 10 最终 `npm run check`、Vitest 6/6、`npm run build` 与 `npm run tauri -- build` 全部通过；Tauri 仅输出既有的 Windows 导入库链接提示。
- 最新 Windows 可执行文件：`src-tauri/target/release/annota.exe`（13,172,224 bytes，SHA256 `1C2B604DD5FB81E28C7F9BF14E0AF4B4CF3AB8CA655EB8CD4581FDBF53509246`）。
- 最新 NSIS 安装包：`src-tauri/target/release/bundle/nsis/Annota_0.1.0_x64-setup.exe`（6,486,888 bytes，SHA256 `C087A7696C370471354BBDA5500D2750ED8E0B6283849E426268E928415FC26E`）。
- Hallmark 最终自评 P5/H5/E5/S5/R5/V4；未引入原始颜色、字体值、`transition-all` 或布局属性过渡。
- 开始并完成 Phase 11：新增空段落离开后清理、颜色拆分按钮与格式栏跨阅读区两列布局。
- TDD 回归覆盖同一编辑器内移开空段落、复用最近字体/背景颜色以及工具栏作为 `reader-surface` 首个直接子级。
- 首次沙箱内 Vitest 被已知 `spawn EPERM` 阻断；允许环境下最终 Vitest 26/26 通过。
- 最终 `npm run check`、`npm run build` 与 `git diff --check` 通过；仅保留 Git 的 LF/CRLF 转换提示。
- 应用内浏览器完成 1280×720 验证：工具栏横跨正文与子文章两列，正文保持居中窄栏，横向溢出为 0，控制台无 warning/error。
- 开始 Phase 12：已读取参考图与现有设置页结构，确定沿用冷白 Cobalt 视觉，在 AI 模型服务分类内实现紧凑的服务商列表与接口配置双栏。
- 已明确本轮只实现页面结构和本地表单交互，不实现真实 API 请求、提示词编辑或 Credential Manager 持久化。
- 已完成 AI 模型服务专用双栏：默认服务商、自定义服务商添加/选择/删除、接口字段、启用与默认状态、API Key 显隐，以及调用 JSON 边界预览。
- `npm run check`、`git diff --check` 与设置页面聚焦 Vitest 通过；聚焦测试覆盖 OpenAI/DeepSeek 切换、Base URL 展示、禁用的测试连接和新增自定义服务。
- 1272px 真实页面视觉检查通过：设置侧栏 210px、服务商列表 232px、配置区约 505.9px，页面无横向溢出。
- Phase 12 完成；真实 API 请求、凭据安全持久化与提示词编辑明确保留为后续实现。
## 2026-07-27 内容显示风格切换
- 已读取 `内容显示.md` 与当前字体偏好实现，确认采用独立显示风格偏好。
- 已建立 Phase 13，下一步定位九类术语在主页、阅读器与导航组件中的实际渲染位置。
- 已定位显示术语的现有渲染位置，并确定由 `App` 统一持有偏好、各页面消费术语映射的实现方式。
- 已完成五套术语映射、默认传统模式、本地持久化与根节点 `data-content-style` 标记。
- 已完成设置页底部左右分栏切换器，并接入主页、全局搜索与阅读器术语显示。
- 第一轮 `npm run check` 与设置页聚焦测试已通过。
- 用户追加自定义风格要求；下一步扩展偏好结构与右侧编辑表单，再进行最终验证。
- 已增加 `+` 自定义风格入口、风格名称和九项术语输入框；留空项自动回退传统术语。
- 自定义风格与当前选择分别持久化，编辑内容会即时影响主页和阅读器显示。
- 自定义扩展后的 `npm run check` 已通过。
- 自定义扩展后的设置页聚焦测试已通过；真实页面默认传统模式与左右分栏已确认，继续检查自定义编辑态几何和溢出。
- 1280×720 自定义编辑态几何检查通过：左列 239px、右列 425px、10 个输入框均未越界，横向溢出为 0。
- 真实页面填写“工作台 / 最近记录”后，主页侧栏即时同步为对应术语，横向溢出仍为 0。
- Phase 13 完成：`npm run check`、设置页聚焦 Vitest、`git diff --check` 与 1280×720 视觉检查均通过。
## 2026-07-27 可编辑快捷键管理
- 已建立 Phase 14，开始盘点全局快捷键、控件局部键盘操作与现有界面提示。
- 首次复合正则查询被 PowerShell 引号解析拦截，已记录并改用拆分查询。
- 已完成盘点：六项应用级快捷键需要统一配置；标准编辑键与控件方向键保持原生行为。
- 已确定由 `App` 持有偏好并下传，设置页负责录入、冲突提示和恢复默认。
- 已新增快捷键注册表、键位格式化、事件匹配、本地加载/保存与冲突检测。
- 已将 App、ReaderPage、BlockEditor、TopologyPanel 的六项硬编码监听接入统一配置。
- 已完成快捷键设置分组、录入状态、重复绑定提示和恢复默认按钮；`npm run check` 通过。
- 聚焦回归已通过：设置页可把拓扑键改为 `Ctrl+G` 并持久化，随后实际监听 `Ctrl+G`；默认 `Ctrl+E` 拓扑回归也通过。
- 已在 1280×720 真实页面确认三组六项快捷键、默认键位、说明文案和恢复默认入口均完整渲染。
- 1280×720 几何复核通过：页面横向溢出为 0，六行及六个 150px 录制按钮均在视口内；录制状态的可访问性标记与提示文案正确。
- Phase 14 完成：`npm run check`、两项快捷键聚焦回归、`git diff --check` 与 1280×720 真实页面检查均通过；本次预览结束后 5173 端口已释放。

## 2026-07-27 模型列表发现与 Zhipu 品牌
- 已建立 Phase 15，目标是接通服务商模型列表发现、下拉选择与手动输入兜底，并修正 GLM 的 Zhipu 图标和品牌色。
- 已确认当前 `@lobehub/icons` 直接提供 `Zhipu` 导出；现有 GLM 错误图标可在原品牌标记组件内定点修复。
- 已确认当前模型字段只有自由文本，下一步增加模型列表地址、异步加载状态、候选下拉与手动输入兜底。
- 已确定采用 Tauri Rust HTTP 命令规避 WebView CORS，浏览器开发模式使用同一请求/解析协议的前端回退。
- 已确认 `Zhipu.Color` 内置使用 `#3859FF`；模型加载交互采用显式按钮触发，下拉选择与手动输入可切换，且不会自动改写当前模型。
- 已新增浏览器/Tauri 双通道模型发现工具，支持 `data`、`models` 或数组形式的模型列表响应，并统一提取 `id/name/model`。
- 已新增 Rust `discover_models` 命令，通过服务商协议设置 Bearer 或 Anthropic 请求头，并限制连接与总请求时长。
- 已将模型服务表单升级为可编辑模型列表地址、显式获取/重新获取、加载与错误提示、下拉选择和手动输入兜底。
- 已将 GLM 服务商的品牌组件由 `ChatGLM` 替换为 `Zhipu.Color`，继续使用统一白色图标底板。
- 前端 TypeScript 检查通过；Rust 桌面端在新增 `reqwest` 依赖和异步模型发现命令后通过 `cargo check`。
- 已确认现有 reduced-motion 全局降级覆盖新增的模型加载旋转图标；下一步只补模型获取到下拉选择的聚焦交互。
- 已补充聚焦交互：模拟 `/models` 返回三项模型，确认获取后切换为下拉框、选择值同步到请求 JSON 预览，并确认 Zhipu 品牌组件存在。
- 修改后的 TypeScript 检查与设置页聚焦回归均通过。
- 已启动本地预览并按浏览器控制规范准备 1280×720 实际布局检查。
- 1280×720 实际布局检查通过：模型发现控件未越界，页面无横向溢出；GLM 的 Zhipu SVG 路径为 `#3859FF` 且图标底板为纯白。
- 首次 Rust 格式检查仅发现两处标准换行差异，已记录并准备按 rustfmt 机械格式化。
- 已按 rustfmt 格式化并通过复查；`git diff --check` 通过，本地预览关闭后 5173 端口已释放。
- 收尾审查修正两个状态细节：初始服务商 ID 与实际 `chatgpt` ID 对齐，模型目录被清空后强制恢复手动输入，避免空下拉框。
- 状态修正后的 TypeScript 检查与设置页模型发现聚焦回归再次通过。
- Phase 15 完成：模型列表 URL、Tauri/浏览器发现通道、下拉选择、手动兜底及 Zhipu `#3859FF` 白底图标均已落地；类型检查、Rust 检查/格式、聚焦交互、差异检查和 1280×720 页面审查通过。

## 2026-07-28 使用模型下拉修复
- 已建立 Phase 16，准备将模型发现动作并入“使用模型”组合选择器，并定位当前请求失败原因。
- 已核对 OpenAI、Gemini OpenAI Compatible 与 DeepSeek 的官方模型列表协议；现有 URL 拼接对这三者成立，问题重点转向交互入口、错误呈现与不同服务商的兼容响应。
- 已定位现有交互问题：获取按钮和模型字段分离，且模型字段会从 input 变成 select；准备改为稳定的异步组合选择器，并扩展 Gemini 原生响应解析。
- 已完成组合选择器主体：文案改为“使用模型”，箭头展开时自动请求，浮层包含状态、刷新、搜索、选项和自定义 ID。
- 已扩展前端/Rust 模型响应解析与 HTTP 错误提示，并增加按服务商请求编号以屏蔽过期响应；首轮 TypeScript 检查通过。
- 已完成组合选择器的 Cobalt 样式：稳定触发器、右侧箭头、浮层状态栏、搜索框、滚动模型列表和自定义 ID 操作。
- 已更新聚焦交互，覆盖 `default-model-chatgpt` 展开自动请求、返回模型显示/选择、请求 JSON 同步及自定义 ID 兜底。
- 组合选择器修改后的 TypeScript 检查与 Rust 格式检查通过。
- 聚焦设置页回归通过，确认模型箭头展开会自动请求并显示选项，选择与自定义 ID 均同步到当前服务商；Rust 桌面端检查通过。
- 已启动本地预览并按浏览器控制规范准备检查“使用模型”组合框及浮层几何。
- 首次隐藏预览进程提前退出，5173 未监听；已记录并切换为可观察输出的开发命令定位。
- 直接前台命令因预览服务为长驻进程而在 1 秒观察窗超时；随后在允许启动子进程的环境后台启动成功，5173 已监听。
- 浏览器不允许从错误内置页导航且页面全局对象不可扩展；已记录并切换为同源静态模型列表数据完成视觉检查。
- 同源模型列表实测成功，组合框自动读取并显示 6 个模型，选择后正常关闭并更新；发现浮层底边超出视口 8px，准备收紧列表高度。
- 收紧后浮层底边为 690px，完整位于 1280×720 视口内且选项列表可内部滚动；网络不可达状态也保持浮层与手动输入入口可用。
- 浏览器请求失败提示已统一为可操作的中文说明，覆盖地址、网络与跨域限制；桌面端仍通过 Tauri HTTP 命令发起请求。
- 最终 `npm run check`、聚焦设置页回归、`cargo fmt --check`、`cargo check` 与 `git diff --check` 均通过；一次性同源模型列表文件已移除，5173 端口已释放。
- Phase 16 完成：`default-model-chatgpt` 对应字段已改为“使用模型”组合选择器，点击右侧箭头会自动请求当前模型列表 URL，并支持加载状态、搜索、选择、刷新、错误反馈和自定义模型 ID 兜底。

## 2026-07-28 内置模型目录与窄窗侧栏
- 已建立 Phase 17，范围包括六家默认服务商的内置模型目录、默认接口字段只读布局，以及较小桌面窗口的侧栏贴边收起/悬停展开。
- 本轮只做针对性类型、交互与差异检查，不扩大到完整端到端测试。
- 已核对六家厂商官方模型页；目录范围限定为当前通用文本/代码生成型号，排除明确弃用、邀请制和专用音视频/图像/嵌入型号。
- 已新增六家默认服务商的内置模型目录，并将 Kimi 默认型号更新为 `kimi-k3`、Claude 默认型号更新为 `claude-sonnet-5`。
- 模型组合框已改为无 Key 只显示内置目录、有 Key 首次展开读取云端；云端成功后显示账号返回列表，空列表或失败时回退内置模型。
- Base URL 与模型列表地址已并入同一双列行；默认服务商的 Base URL、模型列表地址和请求路径改为只读置灰，自定义服务商保持可编辑。
- `home-sidebar` 已增加稳定的外层热区；900px 以下只占 56px 内容宽度，鼠标悬停或键盘聚焦后以裁切动画覆盖展开，768px 以下继续沿用完全隐藏的移动布局。
- 首轮 `npm run check` 通过。
- 聚焦 Vitest 在沙箱内被已知 esbuild `spawn EPERM` 阻断，尚未进入断言；准备在允许启动测试子进程的环境复验。
- 880×720 初次几何测量确认侧栏轨道占 56px、正文从 x=56 开始、隐藏面板本体宽 236px且页面横向溢出为 0；展开条件最终收敛为稳定热区的纯 `:hover`，避免任何点击焦点让侧栏持续遮挡内容。
- 完成最终轻量复核：`npm run check` 通过，设置页聚焦 Vitest 用例 1 项通过（其余 21 项按筛选跳过），`git diff --check` 单独复跑通过。
- 同一窄窗设置页确认 Base URL 与模型列表地址同顶边并排，三项默认接口字段均为只读；同时为横向服务商选择带增加尺寸包含，避免其滚动内容影响页面级宽度计算。

## 2026-07-28 主页与最近浏览滚动切页
- 已建立 Phase 18，并完成现有主页结构、数据模型、滚动容器与响应式规则盘点。
- 已读取 `15_Cpp` 中四篇代表性 Markdown，确认可用真实标题、标签、摘要和正文作为内置演示内容。
- 已确定顶栏固定、双页面纵向滑动、最近浏览独立滚动和顶部二次上滑返回的实现边界。
- 已先补充滚轮边界、时间降序、C++ 演示卡片和时间线结构回归；沙箱内聚焦 Vitest 被已知的 esbuild `spawn EPERM` 阻断，准备在允许环境确认红灯。
- 允许环境中的新增测试按预期红灯：页面区域与 C++ 时间线尚不存在。
- 已完成固定顶栏下的双页面组件骨架、滚轮/触摸/键盘边界切换、最近浏览日期分组和卡片时间标记。
- 已加入“虚函数与动态多态、虚函数表、虚析构函数、模板基础与函数模板”四篇 C++ 主笔记及正文演示数据。
- 聚焦回归 4 项通过，覆盖滚动边界、时间降序、C++ 卡片、筛选和打开/返回笔记。
- 1280×720 真实预览确认首页与时间线视觉层级完整、无横向溢出；发现旧本地缓存遮住新 seed，已增加按固定 ID 的非破坏性 C++ 演示数据补全。
- 真实滚动边界复核通过：最近页内部从 520px 上滑到 340px、再到 0px 时保持当前页，顶部后的下一次上滑才返回首页。
- 已为非活动页面同时设置原生 `inert` 与 `aria-hidden`，避免键盘和辅助技术进入屏幕外页面；相应测试路径已调整为先切换最近浏览再打开卡片。
- Phase 18 完成：完整 Vitest 33 项通过，最终 TypeScript/生产构建通过，`git diff --check` 通过。
- 最终交付包含滚轮/触摸/键盘切页、顶部二次上滑返回、固定时间降序时间线、四篇 C++ 内置演示文章、旧缓存非破坏性补全，以及桌面/窄窗响应式和减少动态效果兼容。

## 2026-07-28 主页信息层级与悬浮操作入口
- 已建立 Phase 19，并完成主页结构、目标选择器、响应式覆盖与相关测试断言盘点。
- 本轮按用户要求不跑完整端到端流程，只做代码审查、类型检查和轻量构建。
- 根据反馈将概览页底部内边距从通用的悬浮按钮预留值覆盖为 8px，`home-scroll-cue` 现在贴近页面实际底边。
- 将原生 `details/summary` 操作入口替换为受控悬浮容器；鼠标进入时向上展开，离开按钮与菜单的整体范围后立即收回，键盘与触控仍可使用。
- 本轮轻量 `npm run check` 通过，未运行完整测试流程。

## 2026-07-28 首页概况横排与操作菜单动效
- 新增 `home-overview-tools`，在桌面宽度将知识库概况和全局搜索放在同一行。
- 搜索栏轨道调整为 42.5%，满足占用 40% 以上空间的要求；移动端在 768px 以下改回单列。
- 操作菜单增加整体上移、两个选项错峰上弹和透明悬浮连接区，鼠标可平稳移入选项后再在离开时收起。
- `npm run check` 与 `git diff --check` 均通过；按用户要求未运行完整全流程测试。

## 2026-07-28 首页资料视图导航
- 已建立 Phase 21；按用户要求并行拆分独立资料页面组件、主页导航集成与轻量结构审查。
- 视觉方向沿用现有冷白 Cobalt 工作台，以目录索引、标签字标和收藏标记作为三类页面各自的结构特征，不引入新的装饰性视觉体系。
- 首页概况单项高度由 72px 收紧为 56px，字号、间距同步缩小；搜索栏轨道从 42.5% 调整为约 35%。
- 侧栏新增文件夹、标签、收藏三个入口，并以独立 `activeHomeView` 状态和 `aria-current` 管理，不干扰主页 overview/recent 滚动状态。
- 新增 `HomeLibraryView`：文件夹按分类聚合、标签按使用量聚合，收藏以明确标注的演示规则生成；三页均通过渲染回调复用现有笔记卡片。
- 新增目录索引、标签索引、收藏说明与空状态样式；768px 以下增加顶部四项资料页签并将分组布局降为单列。
- 更新侧栏结构断言，覆盖三个新入口及对应页面标题。
- `npm run check`、`git diff --check` 与关键选择器静态检索通过；按用户要求未运行完整测试流程。
- 根据追加反馈移除资料页 `1120px` 最大宽度，让文件夹、标签和收藏视图占满内容区的全部可用宽度。

## 2026-07-28 文件夹两级浏览器
- 已建立 Phase 22；文件夹页将从连续分组列表改为“全部文件夹卡片 → 左侧文件夹栏 + 右侧集合内容”的两级交互。
- 当前数据模型以 `Notebook.category` 作为集合键，集合中的主笔记及其文章页面共同构成文件夹内容统计。
- 已实现全部文件夹卡片网格，卡片展示集合名称、笔记数、页面数和最近三项内容标题。
- 点击卡片后切换为左侧竖向文件夹索引与右侧当前文件夹内容；左栏支持返回全部文件夹及直接切换其他集合。
- 右侧继续通过渲染回调复用现有笔记卡片，保持打开阅读器行为一致；窄屏将左侧索引降级为横向滚动栏。
- 已补充轻量结构断言，检查文件夹卡片进入双栏状态、左侧返回入口和右侧笔记内容。
- `npm run check`、`git diff --check` 与关键选择器静态检索通过；未运行完整测试流程。

## 2026-07-28 可定制文件夹集合与属性编辑
- 已按用户要求在重构前创建存档提交 `45b4803 chore: checkpoint before folder collection redesign`。
- 已建立 Phase 23，准备扩展文件夹集合元数据、横向矩形卡片、最近打开内容与属性编辑弹层。
- 视觉方向沿用冷白 Cobalt 知识工作台；文件夹颜色采用窄色带和图标印章表达，信息密度集中在集合状态与最近打开笔记，避免卡片装饰化。
- 本轮按用户要求只执行类型检查、差异检查和必要的静态审查，不运行复杂全流程测试。
- 已确认 `AppStore` 会把完整 `AppData` 持久化到现有本地存储键；文件夹属性适合加入可选集合字段并通过 reducer 更新，而不是只放在会随页面卸载丢失的组件局部状态。
- 已完成实现前设计复核：放弃接近通用仪表盘的正方形统计卡，改用横向档案夹索引卡，并把视觉风险集中在可自定义色的书脊和最近打开记录条。
- 已扩展 `AppData` 与 `AppStore`：新增文件夹名称、颜色、图标、归类和说明字段，旧缓存会合并内置属性，编辑通过 reducer 持久化。
- 已重构文件夹集合视图：卡片主体直接进入文件夹，独立铅笔图标打开属性编辑；卡片显示笔记/页面统计、集合内容和最近打开的文章标题。
- 已加入原生模态编辑页，可编辑名称、预设或自定义颜色、8 种图标、文件夹归类和说明，并提供即时预览。
- 已确认现有 768px 响应式边界和全局 reduced-motion 规则可继续复用；编辑页在窄屏降为单列，集合卡片的信息区同步改为单列。
- 已完成最终轻量审查：`npm run check` 与 `git diff --check` 通过，旧“查看文件夹”提示、旧卡片动作类和无效错峰变量均无残留；按用户要求未运行完整 Vitest 或端到端流程。
- Phase 23 完成：文件夹属性持久化、横向集合卡、最近打开标题、直接进入交互、编辑弹层和响应式动效均已落地。

## 2026-07-28 文件夹 CRUD、批量管理与图标库
- 已建立 Phase 24；当前工作区包含上一轮尚未提交的文件夹属性重构，本轮在该基础上继续实现并保留全部现有改动。
- 已确认项目内部安装并使用 `lucide-react`；文件夹图标选择将扩展为可搜索的应用内图标库，不新增外部图标依赖。
- 删除策略确定为“删除集合属性并把内部笔记移动到未归档”，不级联删除文章或内容。
- 交互方向：常态只显示搜索、批量管理和右下角加号；进入批量模式后再展开选择计数、改色、归类和删除操作，避免常态界面拥挤。
- 已完成现有实现盘点：需要从单条 upsert 扩展批量 store action，并把文件夹集合来源改为 profile 与笔记分类的并集，才能支持空文件夹与安全删除。
- 已实现批量 profile upsert 与安全删除 reducer，新增空文件夹现在可以独立显示；删除集合时内部笔记会转入受保护的“未归档”集合。
- 已加入文件夹查找、批量选择/全选/改色/归类/删除、单项删除按钮和右下角新建加号。
- 属性编辑页已同时支持新建模式，并把图标选择扩展为 20 个可搜索的 Lucide 应用内图标。
- 首轮 TypeScript 与差异检查通过；静态审查进一步发现 seed 属性自动补全会让已删除的内置文件夹重启后复现，准备增加删除墓碑修正持久化语义。
- 已加入 `deletedFolderKeys` 持久化墓碑；内置文件夹删除后不会被旧缓存迁移逻辑重新补回，重新创建或更新同键集合会自动移除墓碑。
- “未归档”被设为受保护的系统集合，不能删除；其他文件夹删除时内部笔记安全转入该集合。
- 最终 `npm run check` 与 `git diff --check` 通过，CRUD、批量操作、新建入口、可搜索图标集合和响应式选择器静态核对通过；按用户要求未运行完整测试流程。
- Phase 24 完成。

## 2026-07-28 文件夹新建按钮悬浮定位修复
- 已确认按钮消失发生在将其 Portal 到 `document.body` 并引入安全区计算之后，创建业务条件和点击处理仍然存在。
- 已撤回 Portal，按钮恢复在 `HomeLibraryView` 内稳定渲染。
- 固定定位改为直接使用桌面间距变量，层级提升到 `z-index: 320`，并显式设置可见性、透明度与指针交互。
- 窄屏继续使用较小的右下角间距，现有悬停上浮与加号旋转动效保持不变。
- TypeScript 检查通过；按用户要求只进行轻量差异与静态审查。
- Phase 25 完成。

## 2026-07-28 文件夹新建入口并入管理工具栏
- 已移除右下角圆形悬浮加号及其固定定位、进入动画与悬停旋转规则。
- 文件夹搜索栏右侧新增操作组，40px 方形加号紧邻“批量管理”，继续复用原有新建文件夹表单。
- 窄屏时搜索栏与操作组分行，操作组仍保持加号和批量管理并排。
- `npm run check`、`git diff --check` 与旧选择器静态检索通过。
- Phase 26 完成。

## 2026-07-29 文件夹图标编辑与整卡批量选择
- 已移除 `library-folder-definition` 说明区，以及集合卡片和详情页上的 `library-folder-delete` 独立删除入口。
- 删除能力继续保留在文件夹属性编辑页和批量管理中，避免破坏现有 CRUD。
- 文件夹卡片左侧图标和详情页标题图标现在都是 `library-folder-edit` 编辑入口，点击直接打开属性编辑页。
- 批量管理模式下整张 `library-folder-card` 通过主按钮切换选中状态；右上角仅保留无指针事件的状态标记。
- `npm run check` 与 `git diff --check` 通过，目标旧类固定字符串检索无残留；按用户要求未运行完整测试流程。
- Phase 27 完成。

## 2026-07-29 文件夹编辑页布局与图标密度
- 已把文件夹预览移动到左侧基础属性列，并放在图标选择器之后。
- 文件夹归类与集合说明已组合到右侧独立属性列，集合说明输入区同步扩高。
- 图标按钮移除可见名称，仅显示 20px 图标；名称继续通过 `aria-label` 和悬停提示保留。
- 图标网格改为 42px 自适应高密度布局，能够在现有宽度中同时展示更多图标。
- 图标搜索框增加实时结果计数、聚焦描边、清除入口和更清晰的纸面层级。
- `npm run check`、`git diff --check` 与可见图标名称静态检查通过；按要求未运行复杂测试流程。
- Phase 28 完成。
