# Annota Windows 桌面应用构建计划

## 目标
在 `project/` 内从零搭建 Windows 优先的 Tauri + React + TypeScript 桌面应用，以 `Index-White.html` 为主页视觉基线、`Demo-Note-Main.html` 为阅读编辑器基线，并按 `Documents/` 的正式指导实现可演示的核心任务流与视觉验证。

## 阶段

### Phase 1：需求与原型梳理
- 状态：complete
- 提取 PRD/SRS/TDD 的 MVP、Windows、数据与交互约束
- 提取两个 HTML 原型的布局、样式与交互
- 明确本轮可完整演示与暂以本地模拟覆盖的能力

### Phase 2：工程骨架与设计系统
- 状态：complete
- 初始化 Vite + React + TypeScript + Tauri 2 工程
- 建立 Windows 窗口、路由、状态与持久化骨架
- 建立与白色原型一致的设计 token 与图标规范

### Phase 3：首页任务流
- 状态：complete
- 实现最近笔记主页、搜索/筛选、创建/导入入口
- 实现打开笔记、收藏/菜单等可见反馈
- 保持 AI 输入区域为装饰性/引导性，不误导为已接通服务

### Phase 4：阅读编辑器任务流
- 状态：complete
- 实现路径导航、可编辑正文、子文章切换
- 实现选区“解释/翻译”生成占位并转为本地演示节点
- 实现右侧子文章列表、拓扑缩放/拖拽/聚焦/全屏
- 实现本地持久化与导入入口

### Phase 5：验证与修正
- 状态：complete
- 运行类型检查、单元/交互测试与生产构建
- 启动页面并截图验证主页、阅读器、关键交互状态
- 修复视觉与交互偏差，完成桌面构建能力核对

### Phase 6：Windows 窗口框架与切换动效
- 状态：complete
- 移除原生窗口装饰，增加可拖拽自定义标题栏与最小化、最大化、关闭控件
- 将初始逻辑窗口修改为 1920×1080，并保持 Windows/WebView2 系统 DPI 缩放
- 为主页、阅读器、文章节点、弹窗与拓扑全屏补充克制的进入/返回动效
- 重新运行类型检查、测试、前端构建、Cargo 检查与桌面构建

### Phase 7：阅读页布局与局部文章动效
- 状态：complete
- 移除应用级路由切换动画，只保留正文阅读区域的文章前进/返回动效
- 将阅读路径改为可由用户拖拽和键盘调整宽度的左侧面板
- 将正文独立居中、子文章列表固定贴靠阅读工作区右侧
- 移除阅读页顶栏中重复的阅读路径，仅保留左侧路径
- 补充自动化测试并完成桌面与响应式视觉验证

### Phase 8：启动路由、阅读区整合与拓扑交互
- 状态：complete
- 每次应用启动时忽略上次阅读位置并默认显示主页
- 将子文章列表并入阅读页面内容区域，在正文右侧形成同一阅读画布
- 允许用户从拓扑图边框拖拽调整面板宽高，并约束最小/最大尺寸
- 按下 `F` 时仅平移拓扑，将当前阅读节点居中且保持现有缩放比例
- 补充自动化测试，并完成类型检查、构建、交互与视觉验证

### Phase 9：阅读画布视觉融合
- 状态：complete
- 以 `Demo-Note-Main.html` 的阅读区关系为基线，保留当前冷白 Cobalt 主题
- 让正文与右侧子文章共享连续背景，以留白和内容层级替代独立侧栏边框
- 保持正文居中阅读尺度、子文章靠右，以及现有局部文章切换动画
- 完成类型检查、测试、生产构建和 1280×720 视觉验证

### Phase 10：主页标题栏层级调整
- 状态：complete
- 保留 `window-titlebar` 作为唯一横跨整个窗口的应用级标题栏
- 移除主页工具栏中重复的品牌入口，将 `home-topbar` 收入右侧内容列
- 让 `home-topbar` 紧贴位于 `home-main` 正上方，侧栏直接从窗口标题栏下方开始
- 补充结构测试，并完成类型检查、构建与桌面/移动布局验证

### Phase 11：编辑空行、颜色快捷应用与全宽格式栏
- 状态：complete
- 新建空段落在光标移到其他段落或编辑器失焦后自动移除，同时至少保留一个可编辑段落
- 字体颜色与背景标注改为拆分按钮：主按钮直接应用最近使用颜色，下拉按钮负责预设色与自选色
- 将 `inline-formatting-toolbar` 提升为 `reader-surface` 首行并跨正文与子文章两列，正文继续保持 760px 最大宽度
- 补充回归测试，并完成 1280×720 实测、完整测试、类型检查和生产构建

### Phase 12：AI 模型服务双栏配置界面
- 状态：complete
- 将“AI 模型服务”内容改为服务商列表与接口配置双栏结构
- 提供默认服务商与自定义服务商的可切换展示，以及 OpenAI Compatible 配置字段
- 明确服务商、提示词、上下文共同组装调用 JSON 的边界，不实现真实 API 请求或提示词编辑
- 完成聚焦交互测试、类型检查和基础响应式审查

## 关键决策
- 所有实现文件只写入 `project/`。
- Windows 桌面容器使用 Tauri 2，前端使用 React + TypeScript。
- 本轮聚焦可完成的本地任务流；真实 OpenAI Compatible 请求、系统文件选择器与压缩包交换若受凭据/桌面环境限制，以清晰的本地模拟或边界说明处理，不伪装为后端已接通。
- 视觉继承 HTML 原型的冷白、石墨文字、细边界、蓝紫选中态和知识树空间隐喻；不另起无关风格。

## 错误记录
- 首次读取 `project/AGENTS.md` 失败：当前 `project/` 为空且不存在该文件。已改为先确认目录状态。
- Vitest 首次启动出现 `spawn EPERM`：符合 Windows 沙箱限制特征，待类型错误修复后以允许的测试命令重跑。
- 首次生产构建被 `tsconfig.node.json` 的 `allowImportingTsExtensions` 与 emit 配置冲突阻断；已删除不需要的选项。
- 第二次类型检查发现 Vite 配置缺少 Node 类型且 `vite.defineConfig` 不识别 Vitest 的 `test` 字段；已改用 `vitest/config` 并补充 `@types/node`。
- `vitest/config` 与项目 Vite 6 插件类型发生双版本冲突；已将测试配置拆到独立 `vitest.config.ts`，生产 Vite 配置恢复使用 `vite` 类型。
- 沙箱外 Vitest 已能启动，但首次测试配置未启用 globals，导致 `describe is not defined`；已在独立配置中启用 `globals`。
- 首次 Cargo 检查完成依赖编译后被 Tauri Windows 资源门禁阻断：缺少 `src-tauri/icons/icon.ico`。已增加 Annota 知识树 SVG 源图，下一步用 Tauri 图标生成器生成平台资源。
- 首次 `tauri build` 在沙箱内被 `spawn EPERM` 阻断；同一命令在允许环境下成功完成 release 编译与 NSIS 打包。
- Phase 6 的首次 Vitest 运行再次被 Windows 沙箱的 esbuild `spawn EPERM` 阻断；代码检查与前端构建已通过，按既有结论改为在允许环境下重跑测试。
- Phase 7 的首次 Vitest 运行再次在加载配置时触发 esbuild `spawn EPERM`；类型检查与生产构建均通过，继续按已验证方案在允许环境下复验。
- Phase 7 沙箱外首次测试发现新增断言使用了不存在的示例标题“组件内存布局”，且 jsdom 未实现 `setPointerCapture`；已改用实际的 Component 子文章标题，并为指针捕获 API 增加能力守卫。
- Phase 7 第二次测试发现子文章标题同时存在于右侧列表与拓扑节点，导致全局角色查询不唯一；已将断言限定在“下一级子文章”侧栏内。
- 新增拖拽断言时发现 jsdom 的 `fireEvent.pointer*` 不保留 `clientX`，计算结果成为 `NaN`；已改用显式定义 `pointerId/clientX` 的合成事件验证同一生产处理链。
- Hallmark 正则扫描再次因 PowerShell 双引号中的转义组合解析失败；已停止复用复合表达式，后续仅使用拆分后的简单查询。
- 应用内浏览器禁止导航到用于多宽度 iframe 验证的 `data:` 包装页；遵守浏览器安全策略未尝试绕过，当前轮视觉验证限定为真实 1280×720 应用页，移动断点保留代码级与构建级核对。
- Hallmark 最终正则扫描首次因 PowerShell 正则字符串中的管道/引号解析为命令管道而失败；改为拆分成多个简单 `rg` 查询，不复用该复合命令。
- 最终尝试读取 Git 状态时确认 `project/` 及其父目录当前不是 Git 工作树；不影响本次文件交付，改用文件级检查与构建产物时间/哈希核对。
- Phase 8 首次 Vitest 运行在加载配置时再次触发 Windows 沙箱 `esbuild spawn EPERM`；按既有验证边界记录，并改用允许启动测试子进程的环境运行同一命令。
- Phase 8 沙箱外首轮 6 项断言均通过，但窗口级 `F` 测试触发阅读器旧快捷键监听器的 `target.matches is not a function` 未处理异常；为非 HTMLElement 事件目标增加类型守卫后重跑。
- Phase 8 首次用 `Start-Process` 启动视觉验证服务器时，当前 PowerShell 进程因同时存在 `Path/PATH` 键而抛出重复字典键异常；没有进程被启动，改用 `-UseNewEnvironment` 与 npm 的绝对路径创建干净子进程。
- `Start-Process -UseNewEnvironment` 仍在 PowerShell 构造进程环境时触发同一重复键异常；停止重复该方法，改用 Windows `Win32_Process.Create` 启动隐藏服务。
- `Win32_Process.Create` 因当前权限策略返回“拒绝访问”，且启动信息对象不暴露 `ShowWindow`；三种系统后台进程方案均未产生监听服务，停止继续尝试，改由受控持久运行环境托管已构建的 `dist` 静态产物。
- Phase 9 首次 Vitest 与 Tauri 构建仍被 Windows 沙箱的 `esbuild spawn EPERM` 阻断；两项均在允许启动构建子进程的环境中使用原命令复验并通过。
- Phase 9 首次 Hallmark 复合正则使用了 `rg` 不支持的负向前瞻；停止复用该表达式，改为分项 `Select-String` 扫描并通过。
- 首次汇总发布产物信息时将 PowerShell `foreach` 直接接到管道导致空管道解析错误；改为先保存 `$rows` 再格式化输出，成功取得尺寸、时间与 SHA256。
- Phase 10 首次 Vitest 仍在加载配置时触发 Windows 沙箱的 `esbuild spawn EPERM`；尚未执行测试断言，将在允许启动子进程的环境中使用相同命令复验。
- Phase 11 首次聚焦 Vitest 同样被 Windows 沙箱的 `esbuild spawn EPERM` 阻断；随后在允许启动测试子进程的环境中完成红绿回归与完整复验。
- Phase 12 首轮 `npm run check` 与 `git diff --check` 已通过；同一审查命令末尾的 `rg` 复合正则因括号未闭合失败，后续改用简单文本查询，不复用该表达式。
- Phase 12 首次隐藏启动 Vite 后未产生 5173 监听，浏览器访问 1420 返回 `ERR_CONNECTION_REFUSED`；随后用 CIM 检查子进程命令行被权限策略拒绝。停止复用无日志启动与 CIM 查询，改为显式日志启动并核对 5173 端口。
- Phase 12 带日志启动确认 5173 已被现有 Vite 进程监听，新启动进程因端口占用退出；复用现有 5173 预览完成视觉检查，未终止不属于本轮的监听进程。
## Phase 13：内容显示风格切换
- 状态：complete
- 依据 `Documents/Output/内容显示.md` 实现五种内容命名风格
- 在“外观与交互”底部增加左右分栏的风格选择与预览
- 默认使用“传统模式”，切换后即时更新界面术语并持久化到本机
- 增加带 `+` 入口的自定义风格，允许逐项定义九类内容术语
- 补充聚焦测试并进行桌面视口实测

### Phase 13 错误记录
- 浏览器联动检查中尝试从页面执行环境读取 `localStorage`，该环境未暴露存储对象并返回 `TypeError`；持久化已由 Vitest 覆盖，视觉检查改为只读取可见界面文本与几何。
## Phase 14：可编辑快捷键管理
- 状态：complete
- 盘点应用所有全局快捷键并建立统一注册表
- 将快捷键监听改为读取用户配置，支持本机持久化
- 在独立“快捷键”设置分类中显示、录入、冲突校验与恢复默认
- 补充聚焦测试并进行桌面布局检查

### Phase 14 错误记录
- 首次只读检索命令中的 PowerShell 双引号与正则组合未闭合，命令在解析阶段失败且未读取或修改文件；后续拆分为简单查询，不复用该表达式。

## Phase 15：模型列表发现与 Zhipu 品牌修正
- 状态：complete
- 核对当前服务商数据结构、模型字段与 LobeHub 图标导入方式
- 为 OpenAI Compatible 服务商增加模型列表接口配置与按需加载
- 将加载结果接入默认模型下拉框，并保留手动模型名称兜底
- 将 GLM 的品牌图标改为 `Zhipu`，品牌色统一为 `#3859FF`
- 进行类型检查、聚焦交互检查与桌面布局审查

### Phase 15 错误记录
- 首次 `cargo fmt --check` 发现新增 Rust 请求构造和命令注册有两处标准换行差异；无逻辑错误，按 rustfmt 结果格式化后重新检查。
- 最终复核命令因 `rg` 未找到残留的 `ChatGLM/is-glm` 而返回退出码 1；这是预期的“无匹配”结果，其余 `git diff --check` 与行号核对均已正常完成。

## Phase 16：使用模型下拉与自动发现修复
- 状态：complete
- 将“默认模型”文案改为“使用模型”
- 将模型字段改为带右侧箭头的组合选择器，点击展开时自动加载模型
- 在同一浮层中呈现加载、错误、空列表、刷新与模型选项
- 修复模型列表请求链并保留手动模型 ID 兜底
- 进行类型检查、聚焦交互与桌面布局检查

### Phase 16 错误记录
- 首次隐藏启动 Vite 的进程立即退出且未监听 5173，浏览器返回 `ERR_CONNECTION_REFUSED`；停止复用无日志启动，改为直接运行开发命令读取实际输出并在需要时申请允许启动构建子进程。
- 首次复用已进入连接错误页的浏览器标签时，URL 安全策略阻止从内置 `data:` 错误页导航；按浏览器规范弃用该标签并新建标签。
- 新标签的页面全局对象不可扩展，无法临时替换 `window.fetch` 模拟成功响应；不继续绕过浏览器约束，改用本地 Vite 同源静态 JSON 作为一次性模型列表预览数据。

## Phase 17：内置模型目录、接口字段锁定与窄窗侧栏
- 状态：complete
- 从六家服务商官方资料整理截至 2026-07-28 的文本生成模型内置目录
- 无 API Key 时直接显示内置目录；有 Key 时请求云端模型列表，并在失败时保留内置兜底
- 将 Base URL 与模型列表地址改为同一行；默认服务商的 Base URL、模型列表地址和请求路径置灰只读
- 在较小桌面窗口中收起 `home-sidebar`，保留左侧热区，鼠标靠近时平滑展开
- 进行类型检查、聚焦交互测试和差异检查

### Phase 17 错误记录
- 首次同时追加三个规划文件时，补丁使用了与 `progress.md` 实际尾行不一致的上下文而未应用；改为读取各文件真实尾部后分别追加。
- 聚焦 Vitest 首次运行在加载配置时触发 Windows 沙箱的 esbuild `spawn EPERM`；尚未执行测试断言，按既有处理方式在允许启动子进程的环境运行同一命令。
- 浏览器几何读取首次在受限页面上下文使用 `instanceof HTMLElement`，该构造器不可用；改为仅做空值检查后完成只读测量。
- 窄窗实测发现普通 `:focus-within` 会让鼠标点击后的侧栏保持覆盖展开；`focus-visible` 在受测 WebView 鼠标点击中也会保留，因此最终只使用稳定热区的 `:hover` 控制展开，确保移开鼠标后必定收起。
- 最终组合审查命令末尾把 `git diff` 管道到 `Select-Object`，管道提前关闭导致命令整体返回退出码 1；此前的 `npm run check` 与 `git diff --check` 均已完成，随后改为分别执行最终复核。

## Phase 18：主页与最近浏览滚动切页
- 状态：complete
- 将主页顶栏下方拆成首页概览与最近浏览两个等高页面，以向上滑入动画切换
- 最近浏览页独立滚动；仅在其滚动到顶部后继续上滑时返回首页概览
- 将最近浏览固定按时间降序分组，用左侧纵向时间线与右侧现有卡片呈现
- 从 `D:\Files\ObsidianInventory\Unity知识库\10_语言与底层\15_Cpp` 整理 C++ 演示文章与主笔记数据
- 补充滚轮边界、时间排序、演示数据和现有卡片交互回归，并完成构建检查

### Phase 18 错误记录
- 新增滚动切页与 C++ 时间线回归后首次运行聚焦 Vitest，在加载配置时被 Windows 沙箱的 esbuild `spawn EPERM` 阻断，尚未执行断言；按项目既有验证方式在允许启动测试子进程的环境复跑。
- 双页面骨架首次类型检查发现当前 `@types/react` 尚未声明 HTML `inert` 属性；改为在 effect 中设置原生元素的 `inert` 布尔属性，避免扩展全局 JSX 类型并保留隐藏页键盘隔离。
- 为非活动页增加 `aria-hidden` 后，完整回归中的边界测试仍尝试按隐藏区域的可访问名称定位，导致 1 项失败；生产行为正常，测试改用稳定的 `.home-recent-page` 容器定位后复验。
- 一次组合差异审查把大段 `git diff` 管道到 `Select-Object`，管道提前关闭并使命令返回退出码 1；后续将类型、差异与行号检查拆分运行，不复用该组合。
- 尝试向预览统一执行会话发送中断字符时，执行后端不支持该进程类型的交互式中断；随后只读检查确认 5173 已无监听，不需要终止其他进程。

## Phase 19：主页信息层级与悬浮操作入口
- 状态：complete
- 移除主页独立顶栏、规划中提示、本地状态说明与最近页返回提示
- 将全局搜索放到主页标题与最近浏览卡片之间
- 在主页概览直接展示最近浏览卡片，并将下滑提示固定到内容末端
- 将新建/导入收纳为右下角圆形加号与向上弹出的操作菜单
- 将下滑提示贴近概览页实际底边，并让悬浮操作菜单在指针移出后立即复位
- 仅进行静态审查、类型检查与轻量生产构建

### Phase 19 错误记录
- 首次从仓库父目录读取规划文件并执行 `git diff --stat`，父目录不是 Git 工作树且规划文件实际位于 `project/`；切换到 `project/` 后继续，不复用父目录 Git 查询。
- 首次用 `rg` 搭配 PowerShell 风格的 `src/*.test.tsx` 通配路径时触发 Windows 路径语法错误；改用 `rg src -g "*.test.tsx"` 完成只读检索。

## Phase 20：主页概况横排与操作菜单动效
- 状态：complete
- 将 `home-ledger` 与主页搜索框放入同一横向布局，搜索框占可分配宽度的 42.5%
- 为悬浮操作菜单增加整体上移、选项错峰上弹与鼠标移动连接区
- 小于等于 768px 时恢复单列布局，遵守减少动态效果偏好
- 仅执行 TypeScript 与静态代码轻量审查

## Phase 21：首页资料视图导航
- 状态：complete
- 收紧 `home-ledger` 单项尺寸，并适当减小同行搜索框占比
- 在主页左侧新增文件夹、标签、收藏导航及对应内容页
- 复用现有笔记数据与卡片交互，建立清晰且响应式的资料浏览样式
- 并行完成独立页面组件、集成与轻量结构审查
- 仅运行 TypeScript、差异检查与必要的静态检索

## Phase 22：文件夹两级浏览器
- 状态：complete
- 将文件夹定义为包含多篇笔记与页面的内容集合
- 初始状态以卡片网格展示全部文件夹
- 打开文件夹后切换为左侧竖向文件夹栏与右侧内容区
- 支持在左栏切换文件夹及返回全部文件夹
- 仅执行 TypeScript、差异检查与必要的轻量结构审查
